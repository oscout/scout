diff --git a/dist/CommandDock-BvMtR8UO.d.ts b/dist/CommandDock-BvMtR8UO.d.ts
index a93acd8d6d9e4a77051061fd5578de4976332a53..3eff7801825fbb66d025dc259e613f10b8f3911a 100644
--- a/dist/CommandDock-BvMtR8UO.d.ts
+++ b/dist/CommandDock-BvMtR8UO.d.ts
@@ -157,6 +157,7 @@ interface StatusBarProps {
     onToggleTerminal?: () => void;
     /** Whether the terminal is currently open */
     isTerminalOpen?: boolean;
+    terminalLabel?: string;
 }
 declare const StatusBar: React__default.FC<StatusBarProps>;

diff --git a/dist/chunk-DN4GBMXZ.js b/dist/chunk-DN4GBMXZ.js
index eb453be035f262149cdb59b21ee336093a840bcd..281edae83aefc531c1c720f68704856b82bfceff 100644
--- a/dist/chunk-DN4GBMXZ.js
+++ b/dist/chunk-DN4GBMXZ.js
@@ -16,6 +16,7 @@ var chromeGripStyle = {
 var TerminalDrawer = ({
   isOpen,
   onClose,
+  closeLabel = "Close console",
   onToggleMaximize,
   isMaximized = false,
   height = 320,
@@ -59,7 +60,7 @@ var TerminalDrawer = ({
     "div",
     {
       ref: drawerRef,
-      "data-hudson-template": "hudson",
+      "data-hudson-terminal-drawer": "true",
       className: `
         fixed left-0 right-0 shadow-[0_-2px_12px_rgba(0,0,0,0.16)] flex flex-col border-t text-card-foreground
         bg-card
@@ -97,8 +98,8 @@ var TerminalDrawer = ({
               {
                 onClick: onClose,
                 className: "p-1.5 rounded hover:bg-destructive/10 text-muted-foreground hover:text-destructive transition-colors",
-                title: "Close console",
-                "aria-label": "Close console",
+                title: closeLabel,
+                "aria-label": closeLabel,
                 children: /* @__PURE__ */ jsx(X, { size: 14 })
               }
             )
diff --git a/dist/chunk-F64WBZGR.js b/dist/chunk-F64WBZGR.js
index 7e17afc57fc63da9c4e93c8f2c490a63ec0dedc5..70fdde1b23cdd43b5013470f2fc230e454e1820e 100644
--- a/dist/chunk-F64WBZGR.js
+++ b/dist/chunk-F64WBZGR.js
@@ -522,7 +522,8 @@ var StatusBar = ({
   status = { label: "READY", color: "emerald" },
   onStatusClick,
   onToggleTerminal,
-  isTerminalOpen = false
+  isTerminalOpen = false,
+  terminalLabel = "Console"
 }) => {
   const [time, setTime] = useState(/* @__PURE__ */ new Date());
   const [vpCopied, setVpCopied] = useState(false);
@@ -637,13 +638,13 @@ var StatusBar = ({
               {
                 onClick: onToggleTerminal,
                 className: `flex items-center gap-1.5 transition-colors focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none rounded ${isTerminalOpen ? "text-accent" : "text-muted-foreground hover:text-foreground"}`,
-                title: "Toggle Console (Ctrl+`)",
+                title: `Toggle ${terminalLabel} (Ctrl+\`)`,
                 children: [
                   /* @__PURE__ */ jsxs("span", { className: "text-[10px]", children: [
                     ">",
                     "_"
                   ] }),
-                  /* @__PURE__ */ jsx("span", { className: "uppercase text-[9px] font-light tracking-[0.18em]", children: "Console" })
+                  /* @__PURE__ */ jsx("span", { className: "uppercase text-[9px] font-light tracking-[0.18em]", children: terminalLabel })
                 ]
               }
             ),
diff --git a/dist/chunk-JTPDIGGW.js b/dist/chunk-JTPDIGGW.js
index 6d52dfc9c0e4236217db3ecce4c7e57eb04360b2..002894b96cf25f3fb07695383c0e25ffed26038b 100644
--- a/dist/chunk-JTPDIGGW.js
+++ b/dist/chunk-JTPDIGGW.js
@@ -191,6 +191,7 @@ function TerminalRelayInner({
     sendInput,
     resize,
     onData,
+    onDimensions,
     connect,
     disconnect,
     controlMode = "owner"
@@ -215,6 +216,7 @@ function TerminalRelayInner({
   const dragCounter = useRef(0);
   const readOnlyRef = useRef(effectiveReadOnly);
   const lastSentDimsRef = useRef(null);
+  const acceptedObserverDimsRef = useRef(null);
   const resizeFrameRef = useRef(null);
   const startConnectTimerRef = useRef(null);
   const onReadyRef = useRef(onReady);
@@ -271,14 +273,17 @@ function TerminalRelayInner({
     const term = termRef.current;
     if (!fit || !term) return null;
     try {
-      fit.fit();
+      const proposed = fit.proposeDimensions();
+      if (!proposed) return null;
+      if (controlMode !== "observe" || !acceptedObserverDimsRef.current) {
+        fit.fit();
+      }
+      emitResize(proposed.cols, proposed.rows);
+      return acceptedObserverDimsRef.current ?? { cols: term.cols, rows: term.rows };
     } catch {
       return null;
     }
-    const dims = { cols: term.cols, rows: term.rows };
-    emitResize(dims.cols, dims.rows);
-    return dims;
-  }, [emitResize]);
+  }, [controlMode, emitResize]);
   const scheduleFit = useCallback(() => {
     if (resizeFrameRef.current !== null) return;
     resizeFrameRef.current = window.requestAnimationFrame(() => {
@@ -533,6 +538,17 @@ function TerminalRelayInner({
     term.options.theme = xtermTheme;
     term.options.minimumContrastRatio = minimumContrastRatio;
   }, [minimumContrastRatio, xtermTheme]);
+  useEffect(() => {
+    if (!ready || !onDimensions) return;
+    onDimensions((cols, rows) => {
+      acceptedObserverDimsRef.current = { cols, rows };
+      const term = termRef.current;
+      if (term && (term.cols !== cols || term.rows !== rows)) {
+        term.resize(cols, rows);
+      }
+    });
+    return () => onDimensions(null);
+  }, [ready, onDimensions]);
   useEffect(() => {
     if (!ready) return;
     onData((data, ack) => {
diff --git a/dist/chunk-ZG2347IS.js b/dist/chunk-ZG2347IS.js
index 28dfb2052257e92da282e0e370b916f64b4cfc67..547eb3fc52ecf4032c207f70b923a58fe3129645 100644
--- a/dist/chunk-ZG2347IS.js
+++ b/dist/chunk-ZG2347IS.js
@@ -74,6 +74,8 @@ function useTerminalRelay(options = {}) {
   const sessionIdRef = useRef(persistedRef.current?.id ?? null);
   const reconnectTokenRef = useRef(persistedRef.current?.reconnectToken ?? null);
   const dataCallbackRef = useRef(null);
+  const dimensionsCallbackRef = useRef(null);
+  const remoteDimensionsRef = useRef(null);
   const subscribersRef = useRef(/* @__PURE__ */ new Set());
   const maxPendingOutput = flowControl?.maxBufferedBytes ?? 512 * 1024;
   const send = useCallback((data) => {
@@ -129,6 +131,11 @@ function useTerminalRelay(options = {}) {
       }
     }
   }, [ackOutput, maxPendingOutput]);
+  const publishDimensions = useCallback((cols, rows) => {
+    if (typeof cols !== "number" || typeof rows !== "number" || cols <= 0 || rows <= 0) return;
+    remoteDimensionsRef.current = { cols, rows };
+    dimensionsCallbackRef.current?.(cols, rows);
+  }, []);
   const closeCurrentSocket = useCallback(() => {
     const ws = wsRef.current;
     if (ws) {
@@ -238,6 +245,7 @@ function useTerminalRelay(options = {}) {
         const msg = JSON.parse(event.data);
         switch (msg.type) {
           case "session:ready":
+            publishDimensions(msg.cols, msg.rows);
             sessionIdRef.current = msg.sessionId;
             reconnectTokenRef.current = typeof msg.reconnectToken === "string" ? msg.reconnectToken : null;
             persistSession(msg.sessionId, reconnectTokenRef.current);
@@ -246,6 +254,9 @@ function useTerminalRelay(options = {}) {
             setError(null);
             setExitCode(null);
             break;
+          case "terminal:dimensions":
+            publishDimensions(msg.cols, msg.rows);
+            break;
           case "session:expired": {
             sessionIdRef.current = null;
             reconnectTokenRef.current = null;
@@ -332,6 +343,11 @@ function useTerminalRelay(options = {}) {
       }
     }
   }, []);
+  const onDimensions = useCallback((cb) => {
+    dimensionsCallbackRef.current = cb;
+    const dimensions = remoteDimensionsRef.current;
+    if (cb && dimensions) cb(dimensions.cols, dimensions.rows);
+  }, []);
   const subscribeData = useCallback((cb) => {
     subscribersRef.current.add(cb);
     return () => {
@@ -369,6 +385,7 @@ function useTerminalRelay(options = {}) {
     controlMode,
     setCwd,
     onData,
+    onDimensions,
     subscribeData,
     sendInput,
     sendLine,
diff --git a/dist/overlays.d.ts b/dist/overlays.d.ts
index 1f5ff15cfb417ac62887f76bc8e3a8680857cd0a..4c860fc4f65f2a84dd0b5e84ca9d8b3021e4468d 100644
--- a/dist/overlays.d.ts
+++ b/dist/overlays.d.ts
@@ -5,6 +5,7 @@ import './types-DQppvHZi.js';
 interface TerminalDrawerProps {
     isOpen: boolean;
     onClose: () => void;
+    closeLabel?: string;
     onToggleMaximize?: () => void;
     isMaximized?: boolean;
     /** Controlled height in px (default 320) */
diff --git a/dist/terminal.d.ts b/dist/terminal.d.ts
index fedb383530bb8728fd4cef7a94460b238b4c990e..1a26f99374f02c27a9d2ebaca482d0e1e72faa41 100644
--- a/dist/terminal.d.ts
+++ b/dist/terminal.d.ts
@@ -60,6 +60,9 @@ interface TerminalSessionReadyMessage {
     reconnectToken?: string;
     reconnected?: boolean;
     capabilities?: string[];
+    /** Server-accepted terminal viewport. May be smaller than the requested grid. */
+    cols?: number;
+    rows?: number;
 }
 interface TerminalSessionExpiredMessage {
     type: 'session:expired';
@@ -75,6 +78,11 @@ interface TerminalDataMessage {
     /** Monotonic sequence for flow-controlled relays. Client ACKs after renderer write. */
     seq?: number;
 }
+interface TerminalDimensionsMessage {
+    type: 'terminal:dimensions';
+    cols: number;
+    rows: number;
+}
 interface TerminalSessionExitMessage {
     type: 'session:exit';
     exitCode?: number;
@@ -83,7 +91,7 @@ interface TerminalSessionExitMessage {
 interface TerminalSessionDetachedMessage {
     type: 'session:detached';
 }
-type TerminalRelayServerMessage = TerminalSessionReadyMessage | TerminalSessionExpiredMessage | TerminalSessionErrorMessage | TerminalDataMessage | TerminalSessionExitMessage | TerminalSessionDetachedMessage;
+type TerminalRelayServerMessage = TerminalSessionReadyMessage | TerminalSessionExpiredMessage | TerminalSessionErrorMessage | TerminalDataMessage | TerminalDimensionsMessage | TerminalSessionExitMessage | TerminalSessionDetachedMessage;
 interface UseTerminalRelayOptions {
     /** WebSocket URL. Defaults to ws://localhost:3600 */
     url?: string;
@@ -125,6 +133,7 @@ interface UseTerminalRelayOptions {
 }
 type TerminalRelayOutputAck = () => void;
 type TerminalRelayOutputHandler = (data: string, ack: TerminalRelayOutputAck) => void;
+type TerminalRelayDimensionsHandler = (cols: number, rows: number) => void;
 interface TerminalRelayHandle {
     /** Connection status */
     status: RelayStatus;
@@ -148,6 +157,8 @@ interface TerminalRelayHandle {
      * trackers) use `subscribeData` instead.
      */
     onData: (cb: TerminalRelayOutputHandler | null) => void;
+    /** Observe the grid accepted by the relay before its corresponding output is rendered. */
+    onDimensions?: (cb: TerminalRelayDimensionsHandler | null) => void;
     /**
      * Subscribe to incoming PTY data without claiming the primary sink slot.
      * Returns an unsubscribe function. Multiple subscribers allowed; called
