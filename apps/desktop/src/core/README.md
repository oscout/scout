Core product logic lives here.

Subareas:
- `broker/` broker-backed communication
- `agents/` local agent lifecycle
- `pairing/` companion device pairing
- `setup/` diagnostics and bootstrapping
- `flows/` cross-domain orchestration
- `context/` environment and workspace context
- `services/` service wiring and bootstrap
